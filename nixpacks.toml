# Run API from server/ when Railway deploys the whole repo (not Expo).

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --prefix server"]

[phases.build]
cmds = ["npm run build --prefix server"]

[start]
cmd = "node server/dist/index.js"
